{
  "name": "portal",
  "description": "No-op alias kept for upstream API parity. Overlay primitives render in the browser top layer via the `popover` attribute and `<dialog>` element instead — see the top_layer component for the underlying mechanism.",
  "authors": ["Dioxus Labs"],
  "exclude": ["variants", "docs.md", "component.json"],
  "cargoDependencies": [
    {
      "name": "dioxus-primitives",
      "git": "https://github.com/DioxusLabs/components"
    }
  ],
  "globalAssets": ["../../../assets/dx-components-theme.css"]
}
